/**
 * @namespace biigle.ate
 * @ngdoc constant
 * @name VOLUME_ID
 * @memberOf biigle.ate
 * @description Fake VOLUME_ID so the images service of biigle.volumes can be reused
 */
angular.module('biigle.ate').constant('VOLUME_ID', 'ate');
