/**
 * The minimap of the canvas element
 *
 * @type {Object}
 */
biigle.$component('annotations.components.labelIndicator', {
    props: {
        label: {
            required: true,
        },
    },
});
