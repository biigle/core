@if ($volume->handle)
    <a href="https://hdl.handle.net/{{$volume->handle}}" class="btn btn-default btn-xs" title="Handle: {{$volume->handle}}"><span class="fa fa-link fa-fw" aria-hidden="true" ></span></a>
@endif
