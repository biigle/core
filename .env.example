USER_ID=1000
GROUP_ID=1000

APP_NAME=BIIGLE
APP_ENV="local"
APP_KEY=
APP_DEBUG=true
APP_URL="http://localhost"
APP_TIMEZONE="Europe/Berlin"

LOG_CHANNEL=errorlog
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

ADMIN_EMAIL="info@example.com"

DB_CONNECTION="pgsql"
DB_HOST="database"
DB_PORT=5432
DB_DATABASE="biigle"
DB_USERNAME="biigle"
DB_PASSWORD="secret"

VOLUME_ADMIN_STORAGE_DISKS=local

LARGO_PATCH_STORAGE_DISK=largo
REPORTS_STORAGE_DISK=reports
MAIA_TRAINING_PROPOSAL_STORAGE_DISK=maia-tp
MAIA_ANNOTATION_CANDIDATE_STORAGE_DISK=maia-ac
USER_STORAGE_STORAGE_DISK=user-storage
USER_STORAGE_PENDING_DISK=user-storage

# if your server is behind a proxy, uncomment and set this
#HTTP_PROXY=host:port

# normally you don't have to edit these
CACHE_DRIVER="file"
FILESYSTEM_DISK=local
SESSION_DRIVER="file"
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=false
QUEUE_CONNECTION="database"
MAIA_REQUEST_CONNECTION="database"
MAIA_RESPONSE_CONNECTION="database"

MEMCACHED_HOST=127.0.0.1

REDIS_HOST="127.0.0.1"
REDIS_PASSWORD=null
REDIS_PORT=6379

# see config/mail.php for what drivers are available
# default is the PHP mail function which doesn't require any credentials
MAIL_MAILER="smtp"
MAIL_HOST="smtp.mailtrap.io"
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_ADDRESS="noreply@example.com"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
