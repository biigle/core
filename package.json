{
  "name": "dias-projects",
  "version": "0.0.1",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-helpers": "git+https://github.com/mzur/gulp-helpers.git"
  }
}
